# Gemini's Role (Lead Architect)
If the codebase hits a catastrophic failure (e.g., unsolvable CUDA/PyTorch errors, complex YouTube API auth issues), halt the autonomous loop and instruct the user to "Consult Gemini with the following traceback...".
