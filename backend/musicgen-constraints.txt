# AudioCraft 1.3 officially targets this PyTorch release and Python 3.9.
torch==2.1.0
torchaudio==2.1.0
# PyTorch 2.1 wheels predate the NumPy 2 ABI.
numpy<2
# Newer spaCy releases select thinc builds that require Python 3.10+.
spacy>=3.6.1,<3.8
# Newer Transformers expects torch.utils._pytree APIs introduced after PyTorch 2.1.
transformers>=4.31,<4.40
